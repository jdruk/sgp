<p></p>

<div class="ui container">
  <div class="ui menu"">
    <a href="#" class="item active"><strong>Theme</strong></a>
    <%= link_to 'Novo', new_theme_path, class: 'item' %>
    <%= link_to 'Editar', edit_theme_path(@theme), class: 'item' %>
    <%= link_to 'Voltar', themes_path, class: 'item' %>
  </div>

  <p>
    <strong>Projeto:</strong>
    <%= @theme.project.name %>
  </p>

    <p>
    <strong>Nome:</strong>
    <%= @theme.name %>
  </p>

    <p>
    <strong>Descrição:</strong>
    <%= @theme.description %>
  </p>
  
</div>
